---
import { getEntry, render } from "astro:content";
import Base from "../layouts/Base.astro";

const page = await getEntry("pages", "404");
const { Content } = await render(page);
---
<Base title={page.data.title} path="/404">
  <Content />
</Base>
